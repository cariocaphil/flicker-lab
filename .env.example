# PostgreSQL connection string for Drizzle (server/API only — not used by the Vite client).
# Example local default; replace user, password, host, and database name as needed.
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/flickerlab
